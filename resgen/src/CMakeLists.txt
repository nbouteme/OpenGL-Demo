LINK_DIRECTORIES(
	${CMAKE_SOURCE_DIRECTORY}
	)

INCLUDE_DIRECTORIES(
	${PROJECT_BINARY_DIR}/include
	${PROJECT_BINARY_DIR}/reslib/include
	)

ADD_EXECUTABLE(${PROJECT_NAME}
	main.cpp
	)

TARGET_LINK_LIBRARIES(${PROJECT_NAME}
	 reslib
	)

ADD_DEPENDENCIES(${PROJECT_NAME} reslib)
